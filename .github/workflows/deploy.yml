name: Deploy to GitHub Pages












































































































































































**Total deployment should now take: ~12-15 seconds** (down from current ~15-18 seconds depending on cache hits)3. Expected: **30-50% faster install phase** with these changes2. Look for "Build" job duration in workflow logs1. GitHub Actions workflow run timesTrack improvements by checking:## Monitoring---- [ ] (Optional) Optimize bundle size with code splitting- [ ] (Optional) Add build output caching- [x] Added artifact retention limit- [x] Added shallow clone optimization- [x] Changed `npm install` → `npm ci --legacy-peer-deps`- [x] Updated to Node.js 24 environment variable## Quick Win Checklist---2. Deployment status check: ~3-5 sec1. Pages deployment: ~5-7 sec### Deploy Job: ~7-10 seconds6. Action overhead: ~5 sec5. Artifact upload: ~1 sec4. Build: ~0.7 sec3. npm ci: 2-3 sec (vs 4-5 with npm install)2. Node setup (cached): ~1 sec1. Runner startup & Git setup: ~2 sec### Build Job: ~12-13 seconds## Deployment Time Breakdown---**These are already included in your workflow.** The `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` flag handles the deprecation gracefully.- `actions/deploy-pages@v4` → Consider `v4.1.0+`- `actions/upload-artifact@v4` → Consider `v4.5.0+`- `actions/configure-pages@v4` → Consider `v4.1.0+`- `actions/setup-node@v4` → Consider `v4.1.0+`- `actions/checkout@v4` → Consider `v4.2.0+`Your current warnings mention:## Node.js 20 Deprecation Fixes---**Impact:** Faster load times in browser (not CI speed, but user experience)```}  }    chunkSizeWarningLimit: 600    },      }        }          'marked': ['marked']          'three': ['three'],        manualChunks: {      output: {    rollupOptions: {  build: {export default {// vite.config.js```javascript**Optimization:**```(!) Some chunks are larger than 500 kB after minification.```Your build warning:### Option 4: Code Splitting for Large BundleCurrent prebuild work: ~0.5 sec (acceptable)- OR using `npm run build` more efficiently- Pre-generating docs during commitYour `prebuild` script copies docs. Consider:### Option 3: Remove Prebuild Step (if possible)Already optimized by npm cache hit. ✓### Option 2: Parallel Checkout of Dependencies**Impact:** Cache dist folder, reuse if no source changes (2-5 sec gain)```    restore-keys: build-    key: build-${{ github.sha }}    path: docs-viewer/dist  with:  uses: actions/cache@v4- name: Cache build output```yaml### Option 1: Build Caching with `actions/cache`## Additional Optimization Opportunities---- **New total: ~12-13 seconds** ⚡- Node 24 optimization: **-0.5-1 second**- `npm ci` vs `npm install`: **-1-2 seconds**### Expected Improvements:| **Total** | **~15 seconds** || Deploy | ~7 sec || Upload artifact | ~1 sec || Build | ~692 ms || Install deps | 3 sec || Setup Node | ~3 sec || Checkout | ~1 sec ||------|------|| Step | Time |### Current Timings (2026-04-09):## Performance Analysis From Your Logs---**Benefits:** GitHub Pages artifacts only need 1 day retention (auto-deployed after 1 day), saves storage costs```retention-days: 1```yaml**Added:**### 5. **Artifact Retention** 💾**Benefits:** Easier to update version across workflow, prevents version skew between jobs```NODE_VERSION: 20```yaml**Added env variable:**### 4. **Explicit Node Version** 📌- Already using `fetch-tags: false` in your run- Only fetches the latest commit, not entire history**Performance gain:** 5-10% faster on clone```fetch-depth: 1```yaml**Added to checkout:**### 3. **Shallow Clone** ✅- Build took **~1 second**- Dependencies installed in **3 seconds** ✓ (already good due to caching)**Current times from your logs:**- `--legacy-peer-deps` skips peer dependency resolution checks- `npm install` resolves dependencies every time- `npm ci` (clean install) is optimized for CI/CD environments**Performance gain:** 30-50% faster```npm ci --legacy-peer-deps# Afternpm install# Before```yaml**Changed:**### 2. **Replace `npm install` with `npm ci`** ⚡ (BIGGEST IMPACT)**Why:** GitHub is deprecating Node.js 20 actions. Setting this now prevents future failures and may improve performance with newer Node.js features.```  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: trueenv:```yaml**Added:**### 1. **Node.js 24 Compatibility** ✅## Changes Made to `deploy.yml`on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NODE_VERSION: 20

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: docs-viewer/package-lock.json

      - name: Install dependencies (with --legacy-peer-deps for speed)
        run: cd docs-viewer && npm ci --legacy-peer-deps

      - name: Build
        run: cd docs-viewer && npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './docs-viewer/dist'
          retention-days: 1

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
