{
  "name": "docs-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "predev": "echo 'Docs list is managed manually in public/docs-list.json'",
    "prebuild": "rm -rf public/docs public/co-creation public/research public/ROOT_README.md && cp -rL ../docs public/docs && cp -rL ../co-creation public/co-creation && cp -rL ../research public/research && cp -f ../README.md public/ROOT_README.md && echo 'Documentation copied. Docs list is managed manually in public/docs-list.json'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/svelte": "5.3.1",
    "jsdom": "29.0.2",
    "svelte": "^5.55.1",
    "vite": "^8.0.4",
    "vitest": "4.1.4"
  },
  "dependencies": {
    "@types/three": "0.183.1",
    "marked": "18.0.0",
    "three": "0.183.2"
  }
}
